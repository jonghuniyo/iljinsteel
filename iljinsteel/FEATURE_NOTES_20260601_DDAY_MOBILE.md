# 2026-06-01 생활 위젯 추가 수정

## 반영 사항

1. 홈 우측 레일에 크게 노출되던 D-day 카운트다운 카드를 제거하고, 우측 하단 `+` 버튼 메뉴에서 `D-day`를 눌렀을 때만 표시되도록 변경했습니다.
2. 월급날은 매월 5일 기준으로 유지하되, 5일이 토요일/일요일이면 전 영업일로 계산하도록 변경했습니다.
3. 홈 상단 버튼 영역에 `이번 주 식단표` 카카오 채널 링크 버튼을 추가했습니다.
4. 모바일 화면에서 홈 레이아웃이 1열로 전환되고 카드 간격/페이지 여백이 줄어들도록 조정했습니다.
5. 모바일 화면에서 좌측 사이드바 여백 때문에 본문이 과하게 밀리는 문제를 완화했습니다.
6. 시세/유가/뉴스/날씨 로딩 실패 문구를 더 명확한 API 연결 실패 또는 응답 없음 안내로 개선했습니다.

## 검증

- `node --check assets/index-fixed-20260601.js` 통과

## 비고

- 외부 API 실시간 응답은 배포 환경에서 확인해야 합니다.
- D-day 사용자 추가 항목과 퇴근시간 설정은 기존과 동일하게 localStorage에 저장됩니다.
